clear

counter = 0;
maxIter = 10000;

% ask user for a and b values

while you picked bad initial guesses
    % tell user and ask for another set of a and b
end


while we have not exceeded the max iterations

    increment counter
    calculate new c
    
    if root is to the right of c
        move an end
    end

    if root is to the left of c
        move an end
    end

    if c is a root
        then we are done
    end
    
end

disp('Root estimate =');
disp(c);
